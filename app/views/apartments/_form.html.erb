<%= form_with(model: [@building, apartment], local: true) do |form| %>
  <% if apartment.errors.any? %>
    <div id="error_explanation" class="card-header">
      <h2><%= pluralize(apartment.errors.count, "error") %> prohibited
        this apartment from being saved:</h2>
      <ul class="list-inline">
        <% apartment.errors.full_messages.each do |message| %>
           <li class="list-inline-item"><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field mb-3">
    <%= form.label :numero,:class =>"form-label"%>
    <%= form.text_field :number,:class =>"form-control"%>
  </div>
    <div class="field mb-3">
    <%= form.label :Edificio,:class =>"form-label"%>
    <%= form.select :building_id, @buildings.map {|building|
    [building.name, building.id] }, prompt: "Seleccione una opción..."%>
  </div>
  <div class="actions text-center">
    <%= form.submit "Crear Departamento",:class =>"btn btn-success"%>
  </div>
<% end %>
